import { memo } from "react";

function StatFilterContent() {
  return (
    <div>
      <h4>Chọn stats:</h4>
      {/* Render danh sách stats ở đây */}
    </div>
  );
}

export default memo(StatFilterContent);
